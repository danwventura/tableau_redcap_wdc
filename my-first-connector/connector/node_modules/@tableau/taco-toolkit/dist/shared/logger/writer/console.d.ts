import { LogLevel } from '../log-level';
import { Writer, WriterConfig } from './writer';
export type ConsoleWriterConfig = WriterConfig;
export declare class ConsoleWriter extends Writer {
    constructor(config: ConsoleWriterConfig);
    log(message: string, level: LogLevel): void;
}
