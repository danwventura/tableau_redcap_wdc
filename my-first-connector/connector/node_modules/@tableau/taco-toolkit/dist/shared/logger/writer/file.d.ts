import { LogLevel } from '../log-level';
import { LogOptions } from '../log-options';
import { Writer, WriterConfig } from './writer';
interface FileWriterConfig extends WriterConfig {
    filename: string;
    dirPath: string;
    sourceName: string;
}
export declare class FileWriter extends Writer {
    private dirPath;
    private baseFilename;
    private writeStream;
    private dateCreated;
    private sourceName;
    constructor(config: FileWriterConfig);
    private rotateFile;
    log(message: string, level: LogLevel, options?: LogOptions): void;
}
export declare function shouldRotateFile(dateCreated: Date): boolean;
export {};
