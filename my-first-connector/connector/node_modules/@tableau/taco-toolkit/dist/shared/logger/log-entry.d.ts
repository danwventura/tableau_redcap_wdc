import { LogLevel } from './log-level';
export interface LogMessage {
    message: string;
    timestamp?: number;
    level?: LogLevel;
}
export interface LogEntry {
    message: string;
    timestamp: number;
    level: LogLevel;
}
export declare function isLogMessage(value: unknown): value is LogMessage;
