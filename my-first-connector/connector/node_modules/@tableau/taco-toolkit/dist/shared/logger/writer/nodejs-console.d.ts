import { LogLevel } from '../log-level';
import { LogOptions } from '../log-options';
import { ConsoleWriter, ConsoleWriterConfig } from './console';
interface NodeJsConsoleWriterConfig extends ConsoleWriterConfig {
    sourceName?: string;
    includeTimeStamp?: boolean;
}
export declare class NodeJsConsoleWriter extends ConsoleWriter {
    private sourceName;
    private includeTimeStamp;
    constructor(config: NodeJsConsoleWriterConfig);
    log(message: string, level: LogLevel, options?: LogOptions): void;
}
export {};
