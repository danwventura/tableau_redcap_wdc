import { LogLevel } from './log-level';
import type { LogOptions } from './log-options';
export default abstract class LogWritter {
    abstract log(msg: unknown, level: LogLevel, options?: LogOptions): void;
    protected addMetadataToLogEntry(msg: string, logLevel: LogLevel | undefined, timestamp: number): string;
}
