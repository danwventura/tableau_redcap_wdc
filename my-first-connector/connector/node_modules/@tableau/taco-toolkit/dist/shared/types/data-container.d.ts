import { DataTable } from './data-table';
import { Metadata } from './metadata';
export interface DataContainer {
    metadata: Metadata;
    tables: DataTable[];
}
export declare function createDataContainer(): DataContainer;
