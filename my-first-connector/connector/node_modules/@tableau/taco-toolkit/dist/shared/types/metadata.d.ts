/**
 * Represents metadata information associated with a data container.
 */
/** @deprecated Will be removed in the later release. */
export type Metadata = {
    name: string;
    sourceType: string;
};
