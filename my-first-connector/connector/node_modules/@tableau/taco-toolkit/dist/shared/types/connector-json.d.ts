import { AuthenticationType } from '../enums/auth-type';
import { Permission } from './permission';
import { Size } from '../../eps/types/position-size';
export type ConnectorJson = {
    name: string;
    displayName?: string;
    version: string;
    'tableau-version': {
        min: string;
    };
    vendor: {
        name: string;
        'support-link': string;
        email: string;
    };
    window: Size;
    permission: Permission;
    auth: AuthenticationType;
    config?: Record<string, unknown>;
};
