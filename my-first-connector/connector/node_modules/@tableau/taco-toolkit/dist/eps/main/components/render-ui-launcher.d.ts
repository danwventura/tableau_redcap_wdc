import { Position, Size } from '../../types/position-size';
export declare class ConnectorAppLauncher {
    private connectionId;
    private url;
    private position;
    private size;
    private proc;
    constructor(connectionId: string, url: string, position: Position, size: Size);
    private getArgs;
    start(): Promise<void>;
    stop(): void;
    /**
     * Remove chrome user directory. Precautionary check to make sure we aren't deleting a random folder
     */
    private deleteUserDir;
    private registerStopSignalsListener;
}
