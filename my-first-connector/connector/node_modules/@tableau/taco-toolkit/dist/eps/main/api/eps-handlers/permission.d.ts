import { Permission } from '../../../../shared/types/permission';
export type PermissionHandlerRequest = {
    connectorClass: string;
    connectionId: string;
    tacoPath?: string;
};
export declare function handleGetPermission({ connectorClass, connectionId, tacoPath, }: PermissionHandlerRequest): Promise<Permission>;
