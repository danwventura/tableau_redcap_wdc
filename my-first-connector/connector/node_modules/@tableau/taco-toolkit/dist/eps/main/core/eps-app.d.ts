import { EpsAppOptions } from '../../types/eps-app-options';
export default class EpsApp {
    private options;
    constructor(options: EpsAppOptions);
    startServices(): Promise<void>;
    private startStaticServerProcess;
}
