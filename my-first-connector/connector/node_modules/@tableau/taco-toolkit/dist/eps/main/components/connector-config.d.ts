import { Size } from '../../types/position-size';
import { Permission } from '../../../shared/types/permission';
import { ConnectorJson } from '../../../shared/types/connector-json';
export declare function getConnectorConfig(connectorJsonPath: string): ConnectorConfig;
/** Represents settings per connector that do not change. */
declare class ConnectorConfig {
    private config;
    constructor(connectorJsonPath: string);
    get windowSize(): Size;
    get auth(): ConnectorJson['auth'] | null;
    get permission(): Permission;
}
export {};
