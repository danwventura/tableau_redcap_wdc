export default class StaticServer {
    private readonly staticServerId;
    private readonly staticServerAppDir;
    private readonly app;
    private server;
    constructor(staticServerId: string, staticServerAppDir: string);
    private createServer;
    private interceptRequests;
    start(port: number): Promise<void>;
    close(): Promise<void>;
}
