/**
 * Send message to parent process (EPS) if IPC channel exists
 */
export declare function sendStaticServerMessage(message: StaticServerMessage): void;
export type StaticServerMessage = SuccessMessage | ErrorMessage;
interface SuccessMessage {
    status: 'success';
}
interface ErrorMessage {
    status: 'error';
    error: Error;
}
export {};
