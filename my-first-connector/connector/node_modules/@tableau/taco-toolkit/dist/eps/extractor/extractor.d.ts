import { DataContainer } from '../../shared/types/data-container';
import { ExtractorOptions } from '../types/extractor-options';
import { NetworkAdapterOptions } from '../modules/network-adapter';
import { PortManager } from '../modules/port-manager';
export default class Extractor {
    cache: DataContainer | null;
    readonly options: ExtractorOptions;
    private readonly pipelineManager;
    private readonly context;
    private extractorId;
    private extractorApi;
    private tempDataManager;
    private networkAdapter;
    constructor(extractorId: string, options: ExtractorOptions, epsInstanceId: string, callerId: string, epsTempDirPath: string, networkAdapterOptions: NetworkAdapterOptions);
    private makeTableNamesUnique;
    start(portManager: PortManager): Promise<void>;
}
