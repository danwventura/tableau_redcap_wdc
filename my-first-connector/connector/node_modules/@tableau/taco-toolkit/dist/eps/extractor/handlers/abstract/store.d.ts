import ExtractorContext from '../../extractor-context';
import { HandlerInput } from '../../../../shared/types/handler-input';
export declare abstract class Store {
    abstract store(context: ExtractorContext): void;
    run(request: HandlerInput, context: ExtractorContext): Promise<void>;
}
