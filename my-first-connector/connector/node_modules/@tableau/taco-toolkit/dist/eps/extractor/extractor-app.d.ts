import { NetworkAdapterOptions } from '../modules/network-adapter';
import { ExtractorOptions, ExtractorPortConfig } from '../types/extractor-options';
import Extractor from './extractor';
interface ExtractorAppOptions {
    epsInstanceId: string;
    callerId: string;
    extractorId: string;
    extractorOptions: ExtractorOptions;
    epsTempDirPath: string;
    networkAdapterOptions: NetworkAdapterOptions;
}
export declare class ExtractorApp {
    extractor: Extractor;
    constructor(options: ExtractorAppOptions);
    start(portConfig: ExtractorPortConfig): Promise<void>;
}
export {};
