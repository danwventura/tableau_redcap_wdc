import { DataTable } from '../../../../shared/types/data-table';
import { SandboxPlugin } from '../sandbox-plugin';
export default class ParquetUtilsPlugin extends SandboxPlugin {
    init(): void;
}
export declare function parse(data: Uint8Array, tableName: string): Promise<DataTable>;
