import { DataRow } from '../../../../shared/types/data-row';
import { SandboxPlugin } from '../sandbox-plugin';
export default class CsvUtilsPlugin extends SandboxPlugin {
    init(): void;
}
export declare function parse(rawData: ArrayBuffer | string, options: {
    hasHeader: boolean;
}): Promise<{
    rows: string[][];
    headers: string[] | undefined;
}>;
export declare function createDataRows(rows: string[][], columnNames: string[]): Promise<DataRow[]>;
