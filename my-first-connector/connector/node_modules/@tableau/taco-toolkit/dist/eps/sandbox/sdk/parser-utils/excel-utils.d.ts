import { DataTable } from '../../../../shared/types/data-table';
import { SandboxPlugin } from '../sandbox-plugin';
export default class ExcelUtilsPlugin extends SandboxPlugin {
    init(): void;
}
export declare function parse(data: Uint8Array): Promise<DataTable[]>;
