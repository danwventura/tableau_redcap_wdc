import { ExecutionState } from '../utils/command';
interface BuildConfig {
    tacoRootPath: string;
    doClean: boolean;
}
export declare function buildTaco(config: BuildConfig, state: ExecutionState): Promise<void>;
export {};
