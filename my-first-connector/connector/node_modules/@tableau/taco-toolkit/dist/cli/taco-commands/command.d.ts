import { Command } from 'commander';
export declare const OPTION_ACCEPT_EULA = "--accept-eula";
/**
 * A function to add command options and arguments
 */
export type CommandSetup = (program: Command) => Command;
export type CommandHandler = (program: Command) => void;
interface CommandConfig {
    handler: CommandHandler;
    withCommonOptions: boolean;
    loggingOptions?: LoggingOptions;
    setup?: CommandSetup;
}
interface LoggingOptions {
    verbose: true;
}
export declare function createCommand(name: string, description: string, config: CommandConfig): Command;
export declare function addLoggingOptions(program: Command, loggingOptions: LoggingOptions | undefined): Command;
export {};
