import { ExecutionState } from '../../taco-commands/utils/command';
export interface CleanConfig {
    tacoRootPath: string;
}
export declare function cleanConnector(tacoRootPath: string, state: ExecutionState): Promise<void>;
